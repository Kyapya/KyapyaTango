(() => {
 const root=document.documentElement;const theme=document.getElementById('themeToggle');const saved=localStorage.getItem('eijiten-theme');if(saved)root.dataset.theme=saved;theme?.addEventListener('click',()=>{root.dataset.theme=root.dataset.theme==='dark'?'light':'dark';localStorage.setItem('eijiten-theme',root.dataset.theme);});
 const cards=[...document.querySelectorAll('.word-card')];const input=document.getElementById('homeSearch');const empty=document.getElementById('homeEmpty');const alphabet=document.getElementById('alphabet');let activeLetter='all';
 function apply(){const q=(input.value||'').trim().toLowerCase();let shown=0;cards.forEach(c=>{const ok=(activeLetter==='all'||c.dataset.letter===activeLetter)&&(!q||c.dataset.search.includes(q));c.style.display=ok?'':'none';if(ok)shown++;});empty.style.display=shown?'none':'block';}
 input.addEventListener('input',apply);alphabet.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;activeLetter=b.dataset.letter;alphabet.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));apply();});
 const data=JSON.parse(document.getElementById('dictionaryData').textContent);const suggestions=document.getElementById('suggestions');input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();if(!q){suggestions.style.display='none';return;}const hits=data.filter(x=>(x.word+' '+x.lead+' '+x.ipa).toLowerCase().includes(q)).slice(0,5);suggestions.innerHTML=hits.map(x=>`<a href="words/${x.slug}.html"><b>${x.word}</b><small>${x.ipa} — ${x.lead}</small></a>`).join('');suggestions.style.display=hits.length?'block':'none';});
 document.addEventListener('click',e=>{if(!e.target.closest('.home-search')&&!e.target.closest('.suggestions'))suggestions.style.display='none';});
})();
